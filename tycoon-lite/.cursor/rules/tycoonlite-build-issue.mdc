---
description: TycoonLite 플러그인 빌드/로드 문제 해결 가이드
globs: "**/*.java"
alwaysApply: false
---

# TycoonLite 플러그인 로드 실패 분석

## 문제 요약

`TycoonLite-1.0.0-SNAPSHOT.jar` 플러그인이 서버에서 **Unresolved compilation problems** 오류로 로드 실패함.

## 누락/해결 필요 클래스 목록

| 패키지 | 클래스 | 상태 |
|--------|--------|------|
| `kr.bapuri.tycoon.bootstrap` | `ListenerRegistry` | 확인 필요 |
| `kr.bapuri.tycoon.bootstrap` | `ServiceRegistry` | 확인 필요 |
| `kr.bapuri.tycoon.economy.vault` | `VaultIntegration` | 확인 필요 |
| `kr.bapuri.tycoon.player` | `PlayerSessionListener` | 확인 필요 |
| `kr.bapuri.tycoon.shop` | `ShopService` | 확인 필요 |
| `kr.bapuri.tycoon.shop` | `ShopGuiManager` | 확인 필요 |
| `kr.bapuri.tycoon.shop` | `ShopNpcRegistry` | 확인 필요 |
| `kr.bapuri.tycoon.shop` | `ShopAdminCommand` | 확인 필요 |
| `kr.bapuri.tycoon.shop.job` | `FisherShop` | 확인 필요 |
| `kr.bapuri.tycoon.economy` | `EcoCommand` | 확인 필요 |

## 해결 체크리스트

1. **컴파일 확인**: `mvn clean compile` 실행하여 빌드 오류 확인
2. **import 검증**: `TycoonPlugin.java`에서 import된 클래스들이 실제 존재하는지 확인
3. **패키지 경로 일치**: 클래스 파일의 package 선언과 실제 폴더 구조 일치 확인
4. **JAR 재빌드**: `mvn clean package`로 새 JAR 생성
5. **배포 확인**: 빌드된 JAR가 올바른 plugins 폴더에 복사되었는지 확인

## 빌드 명령어

```powershell
# tycoon-lite 폴더에서 실행
Set-Location G:\tycoon-lite
mvn clean compile -q 2>&1 | Select-Object -First 50

# 전체 패키지 빌드
mvn clean package -DskipTests
```

## 기타 서버 경고 (참고용)

| 플러그인 | 경고 | 심각도 |
|----------|------|--------|
| **Lands** | `Particle.DUST` 필드 없음 (버전 호환성) | 낮음 |
| **Lands** | Vault 경제 플러그인 미연동 | 중간 |
| **AngelChest** | WorldGuard 완전 통합 실패 | 낮음 |
